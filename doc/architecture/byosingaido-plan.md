# 秒針ガイド機能追加チェックリスト

## 概要

- 計算成功後だけ、結果パネル下部にアナログ秒針ガイドを表示する。
- 計算前は秒針エリアを表示しない。
- 秒針は端末の現在秒に同期し、1秒ごとに刻む。
- 開始/停止ボタンは作らない。
- ガイドは「計算成功時の時刻」を基準に、`約N秒に1滴` のN秒間隔で動く。
- 現在秒針から次の目印までを色付き範囲で示す。
- 2つ先の目印は破線で控えめに表示する。
- 読ませる線は3:1以上のコントラストを保つ。
- 計算成功後は結果パネルへ自動スクロールし、主結果、滴/分、mL/h、条件、秒針ガイドが同じ流れで見える位置に移動する。
- 秒針ガイドはカウントダウンタイマーではなく、滴下間隔を目視で合わせるための補助表示として扱う。
- 入力変更後や入力エラー時は、表示中の結果とガイドが「前回の計算結果」であり、新しい入力はまだ反映されていないことを画面上に表示する。

## 実装状況

- 2026-05-24: フェーズ1〜10とフェーズ13の実装・文書反映を完了。
- 2026-05-24: `app/tests/calculation-test.html` で 36 / 36 件成功を確認。
- 2026-05-24: ブラウザで計算前非表示、計算後表示、毎秒更新、前回結果表示、対象外表示、短間隔ラベル非表示、360x640 / 320x640 の横スクロールなしを確認。
- 2026-05-24: サーバー停止後の再読み込みで、オフライン相当でも秒針ガイド付きの計算ができることを確認。
- 未確認: v1キャッシュ導入済み状態からv2へ更新できることの実機相当確認。

この下のフェーズ別チェックリストは、実装前に作った詳細計画として残す。進捗の正本は本節、`doc/architecture/implementation-plan.md`、`todo/now.md`、`doc/operations/handover.md` を参照する。

## フェーズ1: 計算結果の返却値を拡張する

- [ ] `window.TentekiCalc.calculate()` の `result` にガイド判定用の数値を追加する。
- [ ] 既存の `mainText` を維持する。
- [ ] 既存の `dropsPerMinuteText` を維持する。
- [ ] 既存の `mlPerHourText` を維持する。
- [ ] 既存の `secondsPerDropText` を維持する。
- [ ] `dropsPerMinuteValue` を追加する。
- [ ] `dropsPerMinuteValue` は丸め前の滴/分を数値で返す。
- [ ] `secondsPerDropRaw` を追加する。
- [ ] `secondsPerDropRaw` は `dropsPerMinuteValue > 0` の場合に `60 / dropsPerMinuteValue` を数値で返す。
- [ ] `dropsPerMinuteValue <= 0` の場合、`secondsPerDropRaw` は `null` を返す。
- [ ] `secondsPerDropSeconds` を追加する。
- [ ] `secondsPerDropSeconds` は既存表示と同じく、表示用に丸めた滴/分から算出した整数秒を返す。
- [ ] `1滴/分未満` の場合、`secondsPerDropSeconds` は `null` を返す。
- [ ] `isSecondGuideEligible` を追加する。
- [ ] `isSecondGuideEligible` は `secondsPerDropRaw >= 1` かつ `secondsPerDropSeconds !== null` かつ `secondsPerDropSeconds < 60` の場合だけ `true` にする。
- [ ] アナログガイドを表示する対象は `isSecondGuideEligible === true` の計算結果だけに限定する。
- [ ] `null`、1秒未満相当、60秒以上の場合は盤面を出さず、対象外の補助文だけ表示する。
- [ ] ガイドの間隔表示には `secondsPerDropSeconds` を使う。
- [ ] 1秒未満の除外判定には `secondsPerDropRaw` または `isSecondGuideEligible` を使う。
- [ ] 丸め表示だけでガイド対象可否を決めない。

## フェーズ2: 秒針ガイド状態の純粋関数を追加する

- [ ] `calc.js` にDOM非依存の `getSecondGuideState(anchorMs, intervalSeconds, nowMs)` を追加する。
- [ ] `getSecondGuideState()` を `window.TentekiCalc` から公開する。
- [ ] `app.js` は `getSecondGuideState()` の戻り値をSVGへ反映するだけにする。
- [ ] `getSecondGuideState()` は現在秒針角度を返す。
- [ ] `getSecondGuideState()` は次目印角度を返す。
- [ ] `getSecondGuideState()` は2つ先目印角度を返す。
- [ ] `getSecondGuideState()` は現在位置から次目印までの範囲角度を返す。
- [ ] `getSecondGuideState()` は盤面上ラベルを出すかどうかを返す。
- [ ] N秒が60を割り切らない場合も、固定目盛りを並べない。
- [ ] N秒が60を割り切らない場合も、現在時刻に対する次目印と2つ先目印だけを動的に返す。
- [ ] `getSecondGuideState()` は `anchorMs` のミリ秒を内部で秒境界へ切り捨てる。
- [ ] `getSecondGuideState()` は `nowMs` のミリ秒を内部で秒境界へ切り捨てる。
- [ ] 秒針ガイドは1秒刻みの補助表示とし、小数秒の角度は扱わない。
- [ ] 次目印は常に現在時刻より後の目印にする。
- [ ] `nowMs === anchorMs` の場合でも、次目印は現在位置ではなく次の未来の目印へ進める。
- [ ] `nowMs === anchorMs + intervalSeconds * 1000` の場合でも、次目印は現在位置ではなく次の未来の目印へ進める。

## フェーズ3: 計算実行時の基準時刻をそろえる

- [ ] submit処理の冒頭で `clickedAt = new Date()` を1回だけ作る。
- [ ] `calculate({ now: clickedAt })` を呼び出す。
- [ ] `anchorMs = Math.floor(clickedAt.getTime() / 1000) * 1000` を作る。
- [ ] 計算ボタン押下の同じ時刻を、計算基準時刻とガイド基準時刻に使う。
- [ ] ガイド側は秒目盛りへ合わせる目的でミリ秒だけを切り捨てる。
- [ ] 計算ロジックは従来どおり分単位で行う。
- [ ] 秒針ガイドは計算成功時の `clickedAt` から作った秒境界の `anchorMs` を基準にする視覚補助として扱う。
- [ ] 残り時間や滴下速度の再計算には秒やミリ秒を使わない。

## フェーズ4: ガイドのタイマー管理を実装する

- [ ] `app.js` は `guideTimeoutId` を持つ。
- [ ] `app.js` は `guideRunId` を持つ。
- [ ] `setInterval` は使わない。
- [ ] 次の秒境界に合わせた再帰的 `setTimeout` で更新する。
- [ ] 新しい成功計算時は、既存の `guideTimeoutId` を停止する。
- [ ] 新しい成功計算時は、`guideRunId` を増やしてから新しいガイドを開始する。
- [ ] 各更新コールバックは開始時に受け取った `guideRunId` と現在の `guideRunId` を比較する。
- [ ] 古い更新コールバックは何もせず終了する。
- [ ] 対象外の成功計算時は、既存タイマーを停止する。
- [ ] 対象外の成功計算時は、`guideRunId` を増やす。
- [ ] 対象外の成功計算時は、盤面を出さず対象外の補助文を表示する。
- [ ] エラー時は既存タイマーを止めない。
- [ ] エラー時は前回ガイドを残す。
- [ ] エラー時は状態表示を「前回の計算結果」に切り替える。
- [ ] 新しい条件は、次に計算成功するまでガイドへ反映しない。
- [ ] ガイド更新は初回表示を即時反映する。
- [ ] 初回表示後は次の秒境界に合わせて再帰的に更新する。
- [ ] 毎秒更新するDOMはSVG属性に限定する。
- [ ] 毎秒変わるテキストは作らない。

## フェーズ5: 前回結果表示の状態管理を実装する

- [ ] ユーザー操作で入力欄を変更した時点で、前回結果が表示中なら状態表示を「前回の計算結果」に切り替える。
- [ ] ユーザー操作で滴下係数を変更した時点で、前回結果が表示中なら状態表示を「前回の計算結果」に切り替える。
- [ ] 計算成功後に `resetInputs()` で行うプログラム上のリセットは、前回結果化のトリガーにしない。
- [ ] 計算成功後に `setSelectedDropFactor(DEFAULT_DROP_FACTOR)` で行うプログラム上のリセットは、前回結果化のトリガーにしない。
- [ ] 前回結果化は `input` イベントで行う。
- [ ] 前回結果化はユーザー操作による `change` で行う。
- [ ] 前回結果化は滴下係数ボタンの `click` で行う。
- [ ] 実装時は `isResettingAfterSuccess` のような一時フラグを使ってよい。
- [ ] または、リセット完了後にユーザー操作ハンドラだけを通す構成にしてよい。
- [ ] 成功直後の通常状態を維持する。

## フェーズ6: ガイドUIを追加する

- [ ] ガイドは結果パネル内の条件表示の下に配置する。
- [ ] 計算前はガイド領域を出さない。
- [ ] 計算成功後に安定した余白付きでガイド領域を追加する。
- [ ] ガイド領域には固定の状態表示を置く。
- [ ] 通常時は「表示中の結果に基づくガイドです。」と表示する。
- [ ] 入力変更後またはエラー時は「前回の計算結果に基づくガイドです。新しい条件は計算すると反映されます。」と表示する。
- [ ] 盤面は正方形のSVGで描く。
- [ ] 320px幅でも横スクロールしない固定比率にする。
- [ ] SVG本体は `inline-size: min(100%, 220px)` を基本にする。
- [ ] SVG本体は `block-size: auto` を基本にする。
- [ ] SVG本体は `aspect-ratio: 1 / 1` を基本にする。
- [ ] SVG本体は中央寄せにする。
- [ ] 狭い画面では `inline-size: min(100%, 180px)` まで下げる。
- [ ] 360x640では、計算成功後のスクロール位置で主結果、滴/分、mL/h、条件、状態表示、ガイド盤面の上半分以上が同じ流れで見えるようにする。
- [ ] 条件とガイドの間には少なくとも12pxの余白を置く。
- [ ] ガイド近くにも「目安ガイドです。指示・施設ルールに従って確認してください。」を表示する。

## フェーズ7: ガイドSVGの見た目とアクセシビリティを実装する

- [ ] SVG全体に `aria-hidden="true"` を付ける。
- [ ] SVG全体に `focusable="false"` を付ける。
- [ ] 読み上げ用の説明はSVGではなく、視覚非表示テキストまたは固定の補助文として用意する。
- [ ] 結果パネル全体の `aria-live="polite"` 内で毎秒変わるテキストを更新しない。
- [ ] ガイド状態文と読み上げ用固定文は、状態が変わった時だけ更新する。
- [ ] 同じ状態のまま入力が続く場合は、同じ文を再設定しない。
- [ ] 通常時の読み上げ用固定文は「秒針ガイドを表示しています。次の目印と2つ先の目印を目安にしてください。」にする。
- [ ] 対象外時の読み上げ用固定文は「この計算結果は秒針ガイドの対象外です。」にする。
- [ ] エラー時の読み上げ用固定文は「入力エラーのため、表示中のガイドは前回の計算結果です。」にする。
- [ ] 次目印には「次」のラベルを付ける。
- [ ] 2つ先目印には「2つ先」のラベルを付ける。
- [ ] `secondsPerDropSeconds <= 2` の短間隔では、盤面上ラベルを出さない。
- [ ] `secondsPerDropSeconds <= 2` の短間隔では、盤面外の凡例だけで「実線: 次」「破線: 2つ先」を示す。
- [ ] `secondsPerDropSeconds <= 2` の場合、次目印と2つ先目印は盤面外周の短い目盛りとして描く。
- [ ] 次目印は実線に固定する。
- [ ] 2つ先目印は破線に固定する。
- [ ] 短間隔時の線幅、長さ、外側凡例の位置をCSSで固定する。
- [ ] 1秒ケースを確認する。
- [ ] 2秒ケースを確認する。
- [ ] `secondsPerDropSeconds > 2` の場合も、次目印は常に実線にする。
- [ ] `secondsPerDropSeconds > 2` の場合も、2つ先目印は常に破線にする。
- [ ] 色や薄さだけに依存しない。
- [ ] 基準目盛りは12時、3時、6時、9時の4点に絞って表示する。
- [ ] 細かい秒目盛りは追加しない。
- [ ] 次目印と2つ先目印を優先して読めるようにする。
- [ ] 色付き範囲は安全・危険を示すものに見えないようにする。
- [ ] 補助文で「次の目印までの目安」と説明する。
- [ ] ライトモードで、目印、秒針、範囲、盤面のコントラストを確認する。
- [ ] ダークモードで、目印、秒針、範囲、盤面のコントラストを確認する。
- [ ] 色は既存CSS変数に合わせる。
- [ ] 読ませる線には `--line` をそのまま使わない。
- [ ] ガイド専用に `--guide-line` を追加する。
- [ ] ガイド専用に `--guide-muted-line` を追加する。
- [ ] ガイド専用に `--guide-range` を追加する。
- [ ] 盤面外周、基準目盛り、秒針、次目印、2つ先目印はいずれも背景または盤面面色に対して3:1以上を目安にする。
- [ ] 薄い装飾線だけは `--line` を使ってよい。
- [ ] 秒針と次目印は `--accent` 系にする。
- [ ] 2つ先目印は3:1以上を満たす `--guide-muted-line` の破線にする。
- [ ] 範囲塗りは `--guide-range` を使う。
- [ ] 通常文字は4.5:1以上を目安に確認する。
- [ ] 大きい文字とUI境界は3:1以上を目安に確認する。

## フェーズ8: 自動スクロールとフォーカスを調整する

- [ ] 自動スクロールは計算成功時だけ行う。
- [ ] 結果とガイドDOMを描画してから `requestAnimationFrame` 後にスクロールする。
- [ ] スクロール先は主結果の直前に固定する。
- [ ] 見出しだけでなく主結果が見える位置にする。
- [ ] エラー時は既存どおり最初の不正入力へフォーカスする。
- [ ] エラー時は結果パネルへスクロールしない。
- [ ] 計算成功時は、入力リセット後に結果パネルへフォーカスを移さない。
- [ ] スクリーンリーダー向けには既存の `aria-live="polite"` で結果更新を伝える。
- [ ] キーボード利用者の現在位置を不用意に奪わない。
- [ ] `prefers-reduced-motion` の場合、スムーズスクロールは使わず即時スクロールにする。
- [ ] `prefers-reduced-motion` の場合も、秒針は1秒ごとの位置更新に留める。
- [ ] 点滅や強い強調演出は入れない。

## フェーズ9: PWAキャッシュを更新する

- [ ] Service Workerのキャッシュ名を `tenteki-timer-v2` へ更新する。
- [ ] 新しいファイルを追加する場合は `ASSETS` に必ず追加する。
- [ ] 外部APIを追加しない。
- [ ] 保存APIを追加しない。
- [ ] 通知を追加しない。
- [ ] 音を追加しない。
- [ ] バイブレーションを追加しない。
- [ ] Wake Lockを追加しない。

## フェーズ10: ロジックテストを追加する

- [ ] `app/tests/calculation-test.html` に `dropsPerMinuteValue` の確認を追加する。
- [ ] `app/tests/calculation-test.html` に `secondsPerDropRaw` の確認を追加する。
- [ ] `app/tests/calculation-test.html` に `secondsPerDropSeconds` の確認を追加する。
- [ ] `app/tests/calculation-test.html` に `isSecondGuideEligible` の確認を追加する。
- [ ] 20滴/mLで4秒を確認する。
- [ ] 15滴/mLで6秒を確認する。
- [ ] 60滴/mLで1秒を確認する。
- [ ] `1滴/分未満` では `secondsPerDropSeconds === null` になることを確認する。
- [ ] `1滴/分未満` では `isSecondGuideEligible === false` になることを確認する。
- [ ] 高滴下数で表示上は約1秒でも `secondsPerDropRaw < 1` になるケースを作る。
- [ ] 高滴下数で表示上は約1秒でも `secondsPerDropRaw < 1` になるケースで、`isSecondGuideEligible === false` になることを確認する。
- [ ] 60秒以上になる低滴下数では `isSecondGuideEligible === false` になることを確認する。
- [ ] 60秒以上になる低滴下数ではアナログガイド対象外にできることを確認する。
- [ ] `getSecondGuideState(anchorMs, intervalSeconds, nowMs)` を固定値で確認する。
- [ ] N秒が60を割り切るケースを確認する。
- [ ] N秒が60を割り切らないケースを確認する。
- [ ] 分をまたぐケースを確認する。
- [ ] 2つ先目印が更新されるケースを確認する。
- [ ] `getSecondGuideState()` が `anchorMs` のミリ秒を切り捨てることを固定値で確認する。
- [ ] `getSecondGuideState()` が `nowMs` のミリ秒を切り捨てることを固定値で確認する。
- [ ] `08:00:00.900` を渡しても `08:00:00.000` 基準の角度になることを確認する。
- [ ] `nowMs === anchorMs` の境界値を確認する。
- [ ] `nowMs === anchorMs + intervalSeconds * 1000` の境界値を確認する。
- [ ] 分またぎ直後の境界値を確認する。
- [ ] 境界値でも、次目印が現在位置ではなく次の未来の目印になることを確認する。

## フェーズ11: ブラウザで手動確認する

- [ ] タイマーライフサイクルをブラウザで確認する。
- [ ] 連続して計算成功した場合に古い `setTimeout` 更新が残らないことを確認する。
- [ ] 対象外の成功計算で盤面更新が止まることを確認する。
- [ ] エラー時に前回ガイドは動き続けるが、状態表示が前回結果になることを確認する。
- [ ] 計算前はガイド非表示であることを確認する。
- [ ] 計算成功後は結果パネルへ自動スクロールすることを確認する。
- [ ] 計算成功後に結果とガイドが見えることを確認する。
- [ ] 計算成功後の入力欄リセットでは「前回の計算結果」表示に切り替わらないことを確認する。
- [ ] ユーザーが計算後に入力欄を変更した時だけ「前回の計算結果」表示へ切り替わることを確認する。
- [ ] ユーザーが計算後に滴下係数を変更した時だけ「前回の計算結果」表示へ切り替わることを確認する。
- [ ] 秒針が端末の現在秒と一致することを確認する。
- [ ] 秒針が1秒刻みで進むことを確認する。
- [ ] 入力変更後に前回結果と前回ガイドが残ることを確認する。
- [ ] 入力変更後に「前回の計算結果」であることが表示されることを確認する。
- [ ] 入力エラー時に前回結果と前回ガイドが消えないことを確認する。
- [ ] 入力エラー時に「前回の計算結果」であることが表示されることを確認する。
- [ ] `secondsPerDropSeconds <= 2` では盤面上ラベルを出さないことを確認する。
- [ ] `secondsPerDropSeconds <= 2` では外側凡例で実線/破線の意味が分かることを確認する。
- [ ] 1秒ケースで次目印と2つ先目印を見分けられることを確認する。
- [ ] 2秒ケースで次目印と2つ先目印を見分けられることを確認する。
- [ ] 360x640で横スクロールしないことを確認する。
- [ ] 390x844で横スクロールしないことを確認する。
- [ ] 320px幅で横スクロールしないことを確認する。
- [ ] 360x640で主結果、滴/分、mL/h、条件、状態表示、ガイドが読めることを確認する。
- [ ] 390x844で主結果、滴/分、mL/h、条件、状態表示、ガイドが読めることを確認する。
- [ ] 320px幅で主結果、滴/分、mL/h、条件、状態表示、ガイドが読めることを確認する。
- [ ] 360x640では自動スクロール後にガイド盤面の上半分以上が見えることを確認する。
- [ ] ライトモードで、盤面外周、基準目盛り、秒針、次目印、2つ先目印、色付き範囲、補助文が読めることを確認する。
- [ ] ダークモードで、盤面外周、基準目盛り、秒針、次目印、2つ先目印、色付き範囲、補助文が読めることを確認する。
- [ ] 読ませる線とUI境界は3:1以上を目安に確認する。
- [ ] 通常文字は4.5:1以上を目安に確認する。
- [ ] 計算成功時に結果パネルへフォーカスが移らないことを確認する。
- [ ] エラー時に最初の不正入力へフォーカスする既存挙動が保たれることを確認する。
- [ ] ガイド状態文が状態変化時だけ更新されることを確認する。
- [ ] 読み上げ用固定文が状態変化時だけ更新されることを確認する。
- [ ] 同じ状態の入力継続で同じ文が再通知されないことを確認する。
- [ ] `prefers-reduced-motion` 時にスムーズスクロールが発生しないことを確認する。
- [ ] `prefers-reduced-motion` 時に強い点滅が発生しないことを確認する。

## フェーズ12: PWA更新とオフライン動作を確認する

- [ ] v1キャッシュ導入済み状態からv2へ更新できることを確認する。
- [ ] タブ再起動後に新UIが表示されることを確認する。
- [ ] PWA再起動後に新UIが表示されることを確認する。
- [ ] 初回読み込み後、オフラインでも秒針ガイド付きの画面を表示できることを確認する。
- [ ] 初回読み込み後、オフラインでも秒針ガイド付きで計算できることを確認する。

## フェーズ13: 正本文書と進行管理を更新する

- [ ] `specs/requirements.md` に、秒針ガイドはリアルタイムのカウントダウンタイマーや通知ではなく、計算後の視覚補助であることを追記する。
- [ ] `doc/architecture/overview.md` に、`calc.js` が秒針ガイド状態の純粋関数を持つことを追記する。
- [ ] `doc/architecture/overview.md` に、`app.js` がDOM反映、再帰的 `setTimeout`、`guideRunId` による古い更新の無視を担当することを追記する。
- [ ] `doc/architecture/tech-stack.md` に、SVGを既存Web標準機能として使うことを追記する。
- [ ] `doc/architecture/tech-stack.md` に、`setTimeout` を既存Web標準機能として使うことを追記する。
- [ ] `doc/architecture/tech-stack.md` に、`prefers-reduced-motion` を既存Web標準機能として使うことを追記する。
- [ ] `doc/architecture/implementation-plan.md` に、秒針ガイドの実装、テスト、手動確認、PWA更新確認を追加する。
- [ ] `todo/` に、秒針ガイドの実装、テスト、手動確認、PWA更新確認を追加する。

## 前提と対象外

- 秒針ガイドは医療判断の代替ではなく、既存の注意文言と同じ前提で扱う。
- 数値カウントダウンは出さない。
- 画面維持は今回の範囲外とする。
- 音は今回の範囲外とする。
- 通知は今回の範囲外とする。
- バイブレーションは今回の範囲外とする。
- Wake Lockは今回の範囲外とする。
- 滴下速度の計算自体は既存要件どおり分単位で行う。
- 秒針ガイドだけが、目視補助として秒境界に丸めた現在時刻を使う。
- 計算結果は永続保存しない。
- 入力値は永続保存しない。
- 秒針ガイド状態は永続保存しない。
- Service WorkerのCache APIはアプリ本体ファイルのオフライン利用に限って使う。
