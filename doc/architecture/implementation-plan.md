# 点滴タイマー 実装チェックリスト

## 概要

自然滴下の残量と終了予定時刻から、`mL/h`、`滴/分`、`秒/滴` の目安を計算する個人利用向けPWAを実装する。

正本は次の3文書とする。

- [../../specs/requirements.md](../../specs/requirements.md)
- [overview.md](overview.md)
- [tech-stack.md](tech-stack.md)

## 前提と制約

- HTML / CSS / JavaScript のみで実装する。
- React、ビルドツール、外部API、DB、ユーザーデータ保存APIは追加しない。
- ユーザー入力値、計算結果、利用履歴は永続保存しない。
- Cache APIはService Workerがアプリ本体ファイルをオフライン利用する目的に限って使用する。
- 計算ロジックはDOMから分離し、固定した `now` を渡してテストできる形にする。
- GitHub Pagesの公開元はリポジトリルートとし、アプリURLは `/<repo>/app/src/` とする。
- GitHub Actionsで別ディレクトリへ再配置する手順はMVPでは作らない。
- `app/src/` 配下で動くように、PWA設定は相対パスに固定する。
- UIには「自然滴下の計算補助です。指示・施設ルールに従って確認してください。」を表示する。

## ファイル構成

```text
app/
├── src/
│   ├── index.html
│   ├── style.css
│   ├── calc.js
│   ├── app.js
│   ├── manifest.json
│   ├── service-worker.js
│   └── icons/
│       ├── icon-192.png
│       └── icon-512.png
└── tests/
    └── calculation-test.html
```

- `calc.js` はDOMを触らず、`window.TentekiCalc` に純粋関数を公開する。
- `app.js` はDOM操作とイベント処理だけを担当し、計算は `window.TentekiCalc.calculate()` を呼び出す。

## フェーズ1: 正本文書の整合

- [x] `overview.md` のルート直下配置案を `app/src/` 配置へ更新する。
- [x] `tech-stack.md` のルート直下配置案を `app/src/` 配置へ更新する。
- [x] GitHub Pages公開元はリポジトリルートと明記する。
- [x] アプリURLは `/<repo>/app/src/` と明記する。
- [x] PWAのスコープは `/<repo>/app/src/` ディレクトリ内と明記する。
- [x] 実装計画や関連文書の参照先を、旧文書やアーカイブではなく標準ディレクトリ側にそろえる。

## フェーズ2: 計算ロジック

- [x] `app/src/calc.js` を作成する。
- [x] `window.TentekiCalc.calculate()` を公開する。
- [x] `calculate()` の入力を `volumeInput`、`endTimeValue`、`dropFactor`、`now` に固定する。
- [x] 戻り値を `{ ok, errors, result }` の形に固定する。
- [x] `errors` に `volume`、`endTime`、`dropFactor` を返せるようにする。
- [x] `result` に `mainText`、`dropsPerMinuteText`、`mlPerHourText`、`secondsPerDropText`、`conditionText`、`roundedVolumeMl`、`roundedEndTimeText`、`remainingMinutes` を返せるようにする。
- [x] 残量を数値化する。
- [x] 残量を10mL単位に四捨五入する。
- [x] 丸め後の残量が10mL以上か検証する。
- [x] 丸め後の残量が2500mL以下か検証する。
- [x] 滴下係数は15、20、60のみ受け付ける。
- [x] 滴下係数が15、20、60以外の場合は `ok: false` として `errors.dropFactor` を返す。
- [x] `HH:MM` の終了予定時刻を10分単位に四捨五入する。
- [x] 終了予定時刻の丸めで60分になった場合は時を1つ進める。
- [x] 終了予定時刻が24時を超えた場合は0時に戻す。
- [x] `now` の秒とミリ秒を0にしてから使う。
- [x] `now` と丸め後の終了予定時刻から、当日または翌日の終了予定日時を決める。
- [x] 残り時間分を計算する。
- [x] `mL/h` を計算し、表示用に丸める。
- [x] `滴/分` を計算し、表示用に丸める。
- [x] 表示用の `滴/分` から `秒/滴` を計算する。
- [x] `1mL/h未満` の表示を実装する。
- [x] `1滴/分未満` の場合は主結果を `1滴/分未満` にする。
- [x] `1滴/分未満` の場合は秒/滴を表示しない。
- [x] `1滴/分未満` の場合も `mL/h` と条件表示は通常どおり返す。

## フェーズ3: ロジックテスト

- [x] `app/tests/calculation-test.html` を作成する。
- [x] 外部ライブラリを使わず、ブラウザで開くとテスト結果が表示される形にする。
- [x] テストは固定した `now` を渡して実行する。
- [x] テストを実行時刻に依存させない。
- [x] `123mL -> 120mL` を確認する。
- [x] `125mL -> 130mL` を確認する。
- [x] `2504mL -> 2500mL` を確認する。
- [x] `2505mL -> 2510mLでエラー` を確認する。
- [x] 残量空欄のエラーを確認する。
- [x] 残量0mL以下のエラーを確認する。
- [x] 残量2500mL超のエラーを確認する。
- [x] `08:14 -> 08:10` を確認する。
- [x] `08:15 -> 08:20` を確認する。
- [x] `23:55 -> 00:00` を確認する。
- [x] 終了予定時刻が現在時刻より後の場合の当日判定を確認する。
- [x] 終了予定時刻が現在時刻より前の場合の翌日判定を確認する。
- [x] 終了予定時刻が現在時刻と同じ場合の翌日判定を確認する。
- [x] `mL/h` の丸めを確認する。
- [x] `滴/分` の丸めを確認する。
- [x] `秒/滴` の丸めを確認する。
- [x] `1mL/h未満` を確認する。
- [x] `1滴/分未満` を確認する。
- [x] 15滴/mLの計算を確認する。
- [x] 20滴/mLの計算を確認する。
- [x] 60滴/mLの計算を確認する。

## フェーズ4: UI実装

- [x] `app/src/index.html` を作成する。
- [x] `app/src/style.css` を作成する。
- [x] 画面を1画面で完結させる。
- [x] タイトル `点滴タイマー` を表示する。
- [x] 注意文言 `自然滴下の計算補助です。指示・施設ルールに従って確認してください。` を表示する。
- [x] 残量入力を `input type="number"` で作る。
- [x] 残量入力に `autocomplete="off"` を設定する。
- [x] 終了予定時刻入力を `input type="time"` で作る。
- [x] 終了予定時刻入力に `autocomplete="off"` を設定する。
- [x] 滴下係数ボタンを15 / 20 / 60滴/mLで作る。
- [x] 滴下係数の初期選択を20滴/mLにする。
- [x] 滴下係数は `fieldset` と `legend` で「滴下係数」のグループ名を持たせる。
- [x] 各滴下係数ボタンは `aria-pressed` で選択状態を表す。
- [x] 計算ボタンを作る。
- [x] 結果表示に主結果、滴/分、mL/hを表示する。
- [x] 条件表示に残量、終了予定時刻、残り時間、滴下係数を表示する。
- [x] エラーは該当入力欄の近くに表示する。
- [x] エラー表示に `aria-live` を使う。
- [x] 入力欄に `aria-describedby` を設定する。
- [x] エラー時の入力欄に `aria-invalid` を設定する。
- [x] エラー時は最初の不正入力へフォーカスする。
- [x] 入力修正時は該当入力欄のエラーを消してよい。
- [x] 再計算時に必ず再検証し、不正ならエラーを再表示する。
- [x] 再計算成功時はすべてのエラーを消す。
- [x] 計算成功後は残量入力を空欄に戻す。
- [x] 計算成功後は終了予定時刻入力を空欄に戻す。
- [x] 計算成功後は滴下係数を20滴/mLに戻す。
- [x] 結果と条件表示は次の正常計算まで残す。
- [x] エラー時は前回結果を消さない。

## フェーズ5: PWA実装

- [x] `app/src/manifest.json` を作成する。
- [x] `manifest.json` の `start_url` を `./` にする。
- [x] `manifest.json` の `scope` を `./` にする。
- [x] `manifest.json` の `display` を `standalone` にする。
- [x] `manifest.json` の `name` を `点滴タイマー` にする。
- [x] `manifest.json` の `short_name` を `点滴タイマー` にする。
- [x] `manifest.json` の `background_color` を `#ffffff` にする。
- [x] `manifest.json` の `theme_color` を `#2563eb` にする。
- [x] `manifest.json` に `icons/icon-192.png` を `sizes: 192x192`、`type: image/png` で設定する。
- [x] `manifest.json` に `icons/icon-512.png` を `sizes: 512x512`、`type: image/png` で設定する。
- [x] `app/src/service-worker.js` を作成する。
- [x] `service-worker.js` はCache Firstで実装する。
- [x] `./` をキャッシュ対象にする。
- [x] `./index.html` をキャッシュ対象にする。
- [x] `./style.css` をキャッシュ対象にする。
- [x] `./calc.js` をキャッシュ対象にする。
- [x] `./app.js` をキャッシュ対象にする。
- [x] `./manifest.json` をキャッシュ対象にする。
- [x] `./icons/icon-192.png` をキャッシュ対象にする。
- [x] `./icons/icon-512.png` をキャッシュ対象にする。
- [x] `app/src/index.html` から `./service-worker.js` を登録する。
- [x] キャッシュ名を `tenteki-timer-v1` にする。
- [x] 更新時にキャッシュ名のバージョンを上げる前提にする。
- [x] 更新時に旧キャッシュを削除する処理を入れる。
- [x] 外部サービスを使わず、単色背景に「滴」の1文字を置いた仮アイコンを作る。
- [x] 仮アイコンを192px PNGで作る。
- [x] 仮アイコンを512px PNGで作る。

## フェーズ6: 手動確認

- [x] 必要に応じて `app/src/` をローカルHTTPサーバーで配信する。
- [x] Service Worker確認は `file://` ではなくHTTP配信で行う。
- [x] 320px幅で横スクロールしないことを確認する。
- [x] 主要ボタンの高さが44px以上であることを確認する。
- [x] 秒/滴、滴/分、mL/h、条件欄の表示順が要件どおりであることを確認する。
- [x] `1滴/分未満` 時に主結果エリアへ `1滴/分未満` が表示されることを確認する。
- [x] `1滴/分未満` 時に秒/滴が表示されないことを確認する。
- [x] `1滴/分未満` 時にmL/hと条件欄が残ることを確認する。
- [x] 入力欄近くにエラーが出ることを確認する。
- [x] エラー時は最初の不正入力へフォーカスすることを確認する。
- [x] 入力修正時に該当エラーが消えることを確認する。
- [x] エラー時に前回結果が消えないことを確認する。
- [x] 計算成功後、入力欄がリセットされることを確認する。
- [x] 計算成功後、結果と条件が見て分かる形で残ることを確認する。
- [x] `prefers-color-scheme` でライト/ダークモードが自動切り替えされることを確認する。
- [x] ライト/ダークモードで本文、エラー、選択中ボタン、主結果が読めることを確認する。
- [x] 通常文字4.5:1以上、大きい文字とUI境界3:1以上を目安に確認する。
- [x] 選択状態が色だけに依存しないことを確認する。
- [x] `localStorage` を使っていないことを確認する。
- [x] `sessionStorage` を使っていないことを確認する。
- [x] `IndexedDB` を使っていないことを確認する。
- [x] Cookieを使っていないことを確認する。
- [x] 外部通信がないことを確認する。
- [x] Service Workerが登録されることを確認する。
- [x] 初回読み込み後、オフラインでも表示できることを確認する。
- [x] 初回読み込み後、オフラインでも計算できることを確認する。
- [x] GitHub Pagesの `/app/src/` 配下URLで表示できることを確認する。
- [ ] ホーム画面に追加できることを確認する。

## 完了条件

- [ ] 正本文書の配置方針が `app/src/` に統一されている。
- [ ] `app/src/` にMVPアプリ一式がある。
- [ ] `app/tests/calculation-test.html` で計算ロジックを確認できる。
- [ ] スマホ幅で1画面の計算UIが使える。
- [ ] 残量、終了予定時刻、滴下係数を入力できる。
- [ ] 現在時刻を自動取得し、日付またぎに対応できる。
- [ ] 10mL丸め、10分丸め、`mL/h`、`滴/分`、`秒/滴` の表示が要件どおりである。
- [ ] エラー表示、結果保持、計算成功後リセットが要件どおりである。
- [ ] PWAとしてホーム画面に追加できる。
- [ ] 初回読み込み後にオフライン利用できる。
- [ ] 個人情報、入力値、計算結果、履歴を保存しない。
