# 点滴タイマー 実装チェックリスト

## 概要

自然滴下の残量と終了予定時刻から、`mL/h`、`滴/分`、`秒/滴` の目安を計算する個人利用向けPWAを実装する。

正本は次の3文書とする。

- [../../specs/requirements.md](../../specs/requirements.md)
- [overview.md](overview.md)
- [tech-stack.md](tech-stack.md)

## 前提と制約

- HTML / CSS / JavaScript のみで実装する。
- React、ビルドツール、外部API、DB、ユーザーデータ保存APIは追加しない。
- ユーザー入力値、計算結果、利用履歴は永続保存しない。
- Cache APIはService Workerがアプリ本体ファイルをオフライン利用する目的に限って使用する。
- 計算ロジックはDOMから分離し、固定した `now` を渡してテストできる形にする。
- GitHub Pagesの公開元はリポジトリルートとし、アプリURLは `/<repo>/app/src/` とする。
- GitHub Actionsで別ディレクトリへ再配置する手順はMVPでは作らない。
- `app/src/` 配下で動くように、PWA設定は相対パスに固定する。
- UIには「自然滴下の計算補助です。指示・施設ルールに従って確認してください。」を表示する。

## ファイル構成

```text
app/
├── src/
│   ├── index.html
│   ├── style.css
│   ├── calc.js
│   ├── app.js
│   ├── manifest.json
│   ├── service-worker.js
│   └── icons/
│       ├── icon-192.png
│       └── icon-512.png
└── tests/
    └── calculation-test.html
```

- `calc.js` はDOMを触らず、`window.TentekiCalc` に純粋関数を公開する。
- `app.js` はDOM操作とイベント処理だけを担当し、計算は `window.TentekiCalc.calculate()` を呼び出す。

## フェーズ1: 正本文書の整合

- [ ] `overview.md` のルート直下配置案を `app/src/` 配置へ更新する。
- [ ] `tech-stack.md` のルート直下配置案を `app/src/` 配置へ更新する。
- [ ] GitHub Pages公開元はリポジトリルートと明記する。
- [ ] アプリURLは `/<repo>/app/src/` と明記する。
- [ ] PWAのスコープは `/<repo>/app/src/` ディレクトリ内と明記する。
- [ ] 実装計画や関連文書の参照先を、旧文書やアーカイブではなく標準ディレクトリ側にそろえる。

## フェーズ2: 計算ロジック

- [ ] `app/src/calc.js` を作成する。
- [ ] `window.TentekiCalc.calculate()` を公開する。
- [ ] `calculate()` の入力を `volumeInput`、`endTimeValue`、`dropFactor`、`now` に固定する。
- [ ] 戻り値を `{ ok, errors, result }` の形に固定する。
- [ ] `errors` に `volume`、`endTime`、`dropFactor` を返せるようにする。
- [ ] `result` に `mainText`、`dropsPerMinuteText`、`mlPerHourText`、`secondsPerDropText`、`conditionText`、`roundedVolumeMl`、`roundedEndTimeText`、`remainingMinutes` を返せるようにする。
- [ ] 残量を数値化する。
- [ ] 残量を10mL単位に四捨五入する。
- [ ] 丸め後の残量が10mL以上か検証する。
- [ ] 丸め後の残量が2500mL以下か検証する。
- [ ] 滴下係数は15、20、60のみ受け付ける。
- [ ] 滴下係数が15、20、60以外の場合は `ok: false` として `errors.dropFactor` を返す。
- [ ] `HH:MM` の終了予定時刻を10分単位に四捨五入する。
- [ ] 終了予定時刻の丸めで60分になった場合は時を1つ進める。
- [ ] 終了予定時刻が24時を超えた場合は0時に戻す。
- [ ] `now` の秒とミリ秒を0にしてから使う。
- [ ] `now` と丸め後の終了予定時刻から、当日または翌日の終了予定日時を決める。
- [ ] 残り時間分を計算する。
- [ ] `mL/h` を計算し、表示用に丸める。
- [ ] `滴/分` を計算し、表示用に丸める。
- [ ] 表示用の `滴/分` から `秒/滴` を計算する。
- [ ] `1mL/h未満` の表示を実装する。
- [ ] `1滴/分未満` の場合は主結果を `1滴/分未満` にする。
- [ ] `1滴/分未満` の場合は秒/滴を表示しない。
- [ ] `1滴/分未満` の場合も `mL/h` と条件表示は通常どおり返す。

## フェーズ3: ロジックテスト

- [ ] `app/tests/calculation-test.html` を作成する。
- [ ] 外部ライブラリを使わず、ブラウザで開くとテスト結果が表示される形にする。
- [ ] テストは固定した `now` を渡して実行する。
- [ ] テストを実行時刻に依存させない。
- [ ] `123mL -> 120mL` を確認する。
- [ ] `125mL -> 130mL` を確認する。
- [ ] `2504mL -> 2500mL` を確認する。
- [ ] `2505mL -> 2510mLでエラー` を確認する。
- [ ] 残量空欄のエラーを確認する。
- [ ] 残量0mL以下のエラーを確認する。
- [ ] 残量2500mL超のエラーを確認する。
- [ ] `08:14 -> 08:10` を確認する。
- [ ] `08:15 -> 08:20` を確認する。
- [ ] `23:55 -> 00:00` を確認する。
- [ ] 終了予定時刻が現在時刻より後の場合の当日判定を確認する。
- [ ] 終了予定時刻が現在時刻より前の場合の翌日判定を確認する。
- [ ] 終了予定時刻が現在時刻と同じ場合の翌日判定を確認する。
- [ ] `mL/h` の丸めを確認する。
- [ ] `滴/分` の丸めを確認する。
- [ ] `秒/滴` の丸めを確認する。
- [ ] `1mL/h未満` を確認する。
- [ ] `1滴/分未満` を確認する。
- [ ] 15滴/mLの計算を確認する。
- [ ] 20滴/mLの計算を確認する。
- [ ] 60滴/mLの計算を確認する。

## フェーズ4: UI実装

- [ ] `app/src/index.html` を作成する。
- [ ] `app/src/style.css` を作成する。
- [ ] 画面を1画面で完結させる。
- [ ] タイトル `点滴タイマー` を表示する。
- [ ] 注意文言 `自然滴下の計算補助です。指示・施設ルールに従って確認してください。` を表示する。
- [ ] 残量入力を `input type="number"` で作る。
- [ ] 残量入力に `autocomplete="off"` を設定する。
- [ ] 終了予定時刻入力を `input type="time"` で作る。
- [ ] 終了予定時刻入力に `autocomplete="off"` を設定する。
- [ ] 滴下係数ボタンを15 / 20 / 60滴/mLで作る。
- [ ] 滴下係数の初期選択を20滴/mLにする。
- [ ] 滴下係数は `fieldset` と `legend` で「滴下係数」のグループ名を持たせる。
- [ ] 各滴下係数ボタンは `aria-pressed` で選択状態を表す。
- [ ] 計算ボタンを作る。
- [ ] 結果表示に主結果、滴/分、mL/hを表示する。
- [ ] 条件表示に残量、終了予定時刻、残り時間、滴下係数を表示する。
- [ ] エラーは該当入力欄の近くに表示する。
- [ ] エラー表示に `aria-live` を使う。
- [ ] 入力欄に `aria-describedby` を設定する。
- [ ] エラー時の入力欄に `aria-invalid` を設定する。
- [ ] エラー時は最初の不正入力へフォーカスする。
- [ ] 入力修正時は該当入力欄のエラーを消してよい。
- [ ] 再計算時に必ず再検証し、不正ならエラーを再表示する。
- [ ] 再計算成功時はすべてのエラーを消す。
- [ ] 計算成功後は残量入力を空欄に戻す。
- [ ] 計算成功後は終了予定時刻入力を空欄に戻す。
- [ ] 計算成功後は滴下係数を20滴/mLに戻す。
- [ ] 結果と条件表示は次の正常計算まで残す。
- [ ] エラー時は前回結果を消さない。

## フェーズ5: PWA実装

- [ ] `app/src/manifest.json` を作成する。
- [ ] `manifest.json` の `start_url` を `./` にする。
- [ ] `manifest.json` の `scope` を `./` にする。
- [ ] `manifest.json` の `display` を `standalone` にする。
- [ ] `manifest.json` の `name` を `点滴タイマー` にする。
- [ ] `manifest.json` の `short_name` を `点滴タイマー` にする。
- [ ] `manifest.json` の `background_color` を `#ffffff` にする。
- [ ] `manifest.json` の `theme_color` を `#2563eb` にする。
- [ ] `manifest.json` に `icons/icon-192.png` を `sizes: 192x192`、`type: image/png` で設定する。
- [ ] `manifest.json` に `icons/icon-512.png` を `sizes: 512x512`、`type: image/png` で設定する。
- [ ] `app/src/service-worker.js` を作成する。
- [ ] `service-worker.js` はCache Firstで実装する。
- [ ] `./` をキャッシュ対象にする。
- [ ] `./index.html` をキャッシュ対象にする。
- [ ] `./style.css` をキャッシュ対象にする。
- [ ] `./calc.js` をキャッシュ対象にする。
- [ ] `./app.js` をキャッシュ対象にする。
- [ ] `./manifest.json` をキャッシュ対象にする。
- [ ] `./icons/icon-192.png` をキャッシュ対象にする。
- [ ] `./icons/icon-512.png` をキャッシュ対象にする。
- [ ] `app/src/index.html` から `./service-worker.js` を登録する。
- [ ] キャッシュ名を `tenteki-timer-v1` にする。
- [ ] 更新時にキャッシュ名のバージョンを上げる前提にする。
- [ ] 更新時に旧キャッシュを削除する処理を入れる。
- [ ] 外部サービスを使わず、単色背景に「滴」の1文字を置いた仮アイコンを作る。
- [ ] 仮アイコンを192px PNGで作る。
- [ ] 仮アイコンを512px PNGで作る。

## フェーズ6: 手動確認

- [ ] 必要に応じて `app/src/` をローカルHTTPサーバーで配信する。
- [ ] Service Worker確認は `file://` ではなくHTTP配信で行う。
- [ ] 320px幅で横スクロールしないことを確認する。
- [ ] 主要ボタンの高さが44px以上であることを確認する。
- [ ] 秒/滴、滴/分、mL/h、条件欄の表示順が要件どおりであることを確認する。
- [ ] `1滴/分未満` 時に主結果エリアへ `1滴/分未満` が表示されることを確認する。
- [ ] `1滴/分未満` 時に秒/滴が表示されないことを確認する。
- [ ] `1滴/分未満` 時にmL/hと条件欄が残ることを確認する。
- [ ] 入力欄近くにエラーが出ることを確認する。
- [ ] エラー時は最初の不正入力へフォーカスすることを確認する。
- [ ] 入力修正時に該当エラーが消えることを確認する。
- [ ] エラー時に前回結果が消えないことを確認する。
- [ ] 計算成功後、入力欄がリセットされることを確認する。
- [ ] 計算成功後、結果と条件が見て分かる形で残ることを確認する。
- [ ] `prefers-color-scheme` でライト/ダークモードが自動切り替えされることを確認する。
- [ ] ライト/ダークモードで本文、エラー、選択中ボタン、主結果が読めることを確認する。
- [ ] 通常文字4.5:1以上、大きい文字とUI境界3:1以上を目安に確認する。
- [ ] 選択状態が色だけに依存しないことを確認する。
- [ ] `localStorage` を使っていないことを確認する。
- [ ] `sessionStorage` を使っていないことを確認する。
- [ ] `IndexedDB` を使っていないことを確認する。
- [ ] Cookieを使っていないことを確認する。
- [ ] 外部通信がないことを確認する。
- [ ] Service Workerが登録されることを確認する。
- [ ] 初回読み込み後、オフラインでも表示できることを確認する。
- [ ] 初回読み込み後、オフラインでも計算できることを確認する。
- [ ] GitHub Pagesの `/app/src/` 配下URLで表示できることを確認する。
- [ ] ホーム画面に追加できることを確認する。

## 完了条件

- [ ] 正本文書の配置方針が `app/src/` に統一されている。
- [ ] `app/src/` にMVPアプリ一式がある。
- [ ] `app/tests/calculation-test.html` で計算ロジックを確認できる。
- [ ] スマホ幅で1画面の計算UIが使える。
- [ ] 残量、終了予定時刻、滴下係数を入力できる。
- [ ] 現在時刻を自動取得し、日付またぎに対応できる。
- [ ] 10mL丸め、10分丸め、`mL/h`、`滴/分`、`秒/滴` の表示が要件どおりである。
- [ ] エラー表示、結果保持、計算成功後リセットが要件どおりである。
- [ ] PWAとしてホーム画面に追加できる。
- [ ] 初回読み込み後にオフライン利用できる。
- [ ] 個人情報、入力値、計算結果、履歴を保存しない。
